[project]
name = "chat-app"
version = "0.1.0"
description = "Minimal FastAPI chat app with a stubbed reply agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "uvicorn[standard]>=0.51.0",
]
